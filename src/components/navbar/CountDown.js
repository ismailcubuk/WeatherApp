import React, { useEffect, useState } from 'react'

export default function CountDown() {

    return (
        <div>asd</div>
    )
}



// const [showToast, setShowToast] = useState(false);

// useEffect(() => {
//     setShowToast(true);
//     const timeoutId = setTimeout(() => setShowToast(false), 3000);
//     return () => clearTimeout(timeoutId);
// }, []); // run once on mount

// const bgClass = type === 'success' ? 'bg-green-500' : 'bg-red-500';

{/* <div className={`fixed bottom-4 right-4 p-4 rounded-md text-white ${bgClass} ${showToast ? 'block' : 'hidden'}`}>
message
</div> */}